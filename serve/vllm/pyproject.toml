[project]
name = "vllm-asr"
version = "0.1.0"
description = "vLLM ASR serving for Breeze-ASR"
requires-python = ">=3.10,<3.15"
dependencies = [
    "vllm[audio]>=0.26.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[tool.uv]
dev-dependencies = []
